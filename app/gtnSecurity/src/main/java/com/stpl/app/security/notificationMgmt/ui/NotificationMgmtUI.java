package com.stpl.app.security.notificationMgmt.ui;


import com.stpl.app.security.notificationMgmt.ui.view.NotificationMgmtView;
import com.vaadin.annotations.AutoGenerated;
import com.vaadin.annotations.Theme;
import com.vaadin.annotations.Widgetset;
import com.vaadin.navigator.Navigator;
import com.vaadin.server.VaadinRequest;
import com.vaadin.server.VaadinSession;
import com.vaadin.ui.UI;
import org.osgi.service.component.annotations.Component;
import org.osgi.service.component.annotations.ServiceScope;

@SuppressWarnings("serial")
@Theme("stpl")
@Widgetset("com.stpl.widgetset.vaadin.widgetset.AppWidgetSet") 
@Component(service = UI.class, property = {
        "com.liferay.portlet.display-category=Security",
        "javax.portlet.name=NotificationMgmt",
        "javax.portlet.display-name=Notification Management",
        "com.vaadin.osgi.liferay.portlet-ui=true"}, scope = ServiceScope.PROTOTYPE)
// @PreserveOnRefresh
public class NotificationMgmtUI extends UI {

	@AutoGenerated
	Navigator navigator;

	@Override
	protected void init(VaadinRequest request) {
            addStyleName("bootstrap");
            addStyleName("bootstrap-bb");
            String userId = request.getRemoteUser();
            VaadinSession.getCurrent().setAttribute("userId", userId);
		navigator = new Navigator(this, this);
		navigator.addView(NotificationMgmtView.NAME,new NotificationMgmtView());
		

	}
}
