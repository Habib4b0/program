package com.stpl.app.model.impl;

import com.stpl.app.model.FormulaDetailsMaster;
import com.stpl.app.service.FormulaDetailsMasterLocalServiceUtil;

import com.stpl.portal.kernel.exception.SystemException;

/**
 * The extended model base implementation for the FormulaDetailsMaster service. Represents a row in the &quot;FORMULA_DETAILS_MASTER&quot; database table, with each column mapped to a property of this class.
 *
 * <p>
 * This class exists only as a container for the default extended model level methods generated by ServiceBuilder. Helper methods and all application logic should be put in {@link FormulaDetailsMasterImpl}.
 * </p>
 *
 * @author
 * @see FormulaDetailsMasterImpl
 * @see com.stpl.app.model.FormulaDetailsMaster
 * @generated
 */
public abstract class FormulaDetailsMasterBaseImpl
    extends FormulaDetailsMasterModelImpl implements FormulaDetailsMaster {
    /*
     * NOTE FOR DEVELOPERS:
     *
     * Never modify or reference this class directly. All methods that expect a formula details master model instance should use the {@link FormulaDetailsMaster} interface instead.
     */
    @Override
    public void persist() throws SystemException {
        if (this.isNew()) {
            FormulaDetailsMasterLocalServiceUtil.addFormulaDetailsMaster(this);
        } else {
            FormulaDetailsMasterLocalServiceUtil.updateFormulaDetailsMaster(this);
        }
    }
}
