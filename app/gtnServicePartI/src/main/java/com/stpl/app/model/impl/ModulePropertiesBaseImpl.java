package com.stpl.app.model.impl;

import com.stpl.app.model.ModuleProperties;
import com.stpl.app.service.ModulePropertiesLocalServiceUtil;

import com.stpl.portal.kernel.exception.SystemException;

/**
 * The extended model base implementation for the ModuleProperties service. Represents a row in the &quot;MODULE_PROPERTIES&quot; database table, with each column mapped to a property of this class.
 *
 * <p>
 * This class exists only as a container for the default extended model level methods generated by ServiceBuilder. Helper methods and all application logic should be put in {@link ModulePropertiesImpl}.
 * </p>
 *
 * @author
 * @see ModulePropertiesImpl
 * @see com.stpl.app.model.ModuleProperties
 * @generated
 */
public abstract class ModulePropertiesBaseImpl extends ModulePropertiesModelImpl
    implements ModuleProperties {
    /*
     * NOTE FOR DEVELOPERS:
     *
     * Never modify or reference this class directly. All methods that expect a module properties model instance should use the {@link ModuleProperties} interface instead.
     */
    @Override
    public void persist() throws SystemException {
        if (this.isNew()) {
            ModulePropertiesLocalServiceUtil.addModuleProperties(this);
        } else {
            ModulePropertiesLocalServiceUtil.updateModuleProperties(this);
        }
    }
}
