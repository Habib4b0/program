package com.stpl.app.model.impl;

import com.stpl.app.model.NaProjDetails;
import com.stpl.app.service.NaProjDetailsLocalServiceUtil;

import com.stpl.portal.kernel.exception.SystemException;

/**
 * The extended model base implementation for the NaProjDetails service. Represents a row in the &quot;NA_PROJ_DETAILS&quot; database table, with each column mapped to a property of this class.
 *
 * <p>
 * This class exists only as a container for the default extended model level methods generated by ServiceBuilder. Helper methods and all application logic should be put in {@link NaProjDetailsImpl}.
 * </p>
 *
 * @author
 * @see NaProjDetailsImpl
 * @see com.stpl.app.model.NaProjDetails
 * @generated
 */
public abstract class NaProjDetailsBaseImpl extends NaProjDetailsModelImpl
    implements NaProjDetails {
    /*
     * NOTE FOR DEVELOPERS:
     *
     * Never modify or reference this class directly. All methods that expect a na proj details model instance should use the {@link NaProjDetails} interface instead.
     */
    @Override
    public void persist() throws SystemException {
        if (this.isNew()) {
            NaProjDetailsLocalServiceUtil.addNaProjDetails(this);
        } else {
            NaProjDetailsLocalServiceUtil.updateNaProjDetails(this);
        }
    }
}
