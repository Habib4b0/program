package com.stpl.app.model.impl;

import com.stpl.app.model.ChProjectionDiscount;
import com.stpl.app.service.ChProjectionDiscountLocalServiceUtil;

import com.stpl.portal.kernel.exception.SystemException;

/**
 * The extended model base implementation for the ChProjectionDiscount service. Represents a row in the &quot;CH_PROJECTION_DISCOUNT&quot; database table, with each column mapped to a property of this class.
 *
 * <p>
 * This class exists only as a container for the default extended model level methods generated by ServiceBuilder. Helper methods and all application logic should be put in {@link ChProjectionDiscountImpl}.
 * </p>
 *
 * @author
 * @see ChProjectionDiscountImpl
 * @see com.stpl.app.model.ChProjectionDiscount
 * @generated
 */
public abstract class ChProjectionDiscountBaseImpl
    extends ChProjectionDiscountModelImpl implements ChProjectionDiscount {
    /*
     * NOTE FOR DEVELOPERS:
     *
     * Never modify or reference this class directly. All methods that expect a ch projection discount model instance should use the {@link ChProjectionDiscount} interface instead.
     */
    @Override
    public void persist() throws SystemException {
        if (this.isNew()) {
            ChProjectionDiscountLocalServiceUtil.addChProjectionDiscount(this);
        } else {
            ChProjectionDiscountLocalServiceUtil.updateChProjectionDiscount(this);
        }
    }
}
