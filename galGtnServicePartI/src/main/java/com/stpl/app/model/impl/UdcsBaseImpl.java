package com.stpl.app.model.impl;

import com.stpl.app.model.Udcs;
import com.stpl.app.service.UdcsLocalServiceUtil;

import com.stpl.portal.kernel.exception.SystemException;

/**
 * The extended model base implementation for the Udcs service. Represents a row in the &quot;UDCS&quot; database table, with each column mapped to a property of this class.
 *
 * <p>
 * This class exists only as a container for the default extended model level methods generated by ServiceBuilder. Helper methods and all application logic should be put in {@link UdcsImpl}.
 * </p>
 *
 * @author
 * @see UdcsImpl
 * @see com.stpl.app.model.Udcs
 * @generated
 */
public abstract class UdcsBaseImpl extends UdcsModelImpl implements Udcs {
    /*
     * NOTE FOR DEVELOPERS:
     *
     * Never modify or reference this class directly. All methods that expect a udcs model instance should use the {@link Udcs} interface instead.
     */
    @Override
    public void persist() throws SystemException {
        if (this.isNew()) {
            UdcsLocalServiceUtil.addUdcs(this);
        } else {
            UdcsLocalServiceUtil.updateUdcs(this);
        }
    }
}
