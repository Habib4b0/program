package com.stpl.app.model.impl;

import com.stpl.app.model.DeductionDetails;
import com.stpl.app.service.DeductionDetailsLocalServiceUtil;

import com.stpl.portal.kernel.exception.SystemException;

/**
 * The extended model base implementation for the DeductionDetails service. Represents a row in the &quot;DEDUCTION_DETAILS&quot; database table, with each column mapped to a property of this class.
 *
 * <p>
 * This class exists only as a container for the default extended model level methods generated by ServiceBuilder. Helper methods and all application logic should be put in {@link DeductionDetailsImpl}.
 * </p>
 *
 * @author
 * @see DeductionDetailsImpl
 * @see com.stpl.app.model.DeductionDetails
 * @generated
 */
public abstract class DeductionDetailsBaseImpl extends DeductionDetailsModelImpl
    implements DeductionDetails {
    /*
     * NOTE FOR DEVELOPERS:
     *
     * Never modify or reference this class directly. All methods that expect a deduction details model instance should use the {@link DeductionDetails} interface instead.
     */
    @Override
    public void persist() throws SystemException {
        if (this.isNew()) {
            DeductionDetailsLocalServiceUtil.addDeductionDetails(this);
        } else {
            DeductionDetailsLocalServiceUtil.updateDeductionDetails(this);
        }
    }
}
