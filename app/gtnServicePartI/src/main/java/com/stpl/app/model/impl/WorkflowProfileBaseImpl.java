package com.stpl.app.model.impl;

import com.stpl.app.model.WorkflowProfile;
import com.stpl.app.service.WorkflowProfileLocalServiceUtil;

import com.stpl.portal.kernel.exception.SystemException;

/**
 * The extended model base implementation for the WorkflowProfile service. Represents a row in the &quot;WORKFLOW_PROFILE&quot; database table, with each column mapped to a property of this class.
 *
 * <p>
 * This class exists only as a container for the default extended model level methods generated by ServiceBuilder. Helper methods and all application logic should be put in {@link WorkflowProfileImpl}.
 * </p>
 *
 * @author
 * @see WorkflowProfileImpl
 * @see com.stpl.app.model.WorkflowProfile
 * @generated
 */
public abstract class WorkflowProfileBaseImpl extends WorkflowProfileModelImpl
    implements WorkflowProfile {
    /*
     * NOTE FOR DEVELOPERS:
     *
     * Never modify or reference this class directly. All methods that expect a workflow profile model instance should use the {@link WorkflowProfile} interface instead.
     */
    @Override
    public void persist() throws SystemException {
        if (this.isNew()) {
            WorkflowProfileLocalServiceUtil.addWorkflowProfile(this);
        } else {
            WorkflowProfileLocalServiceUtil.updateWorkflowProfile(this);
        }
    }
}
