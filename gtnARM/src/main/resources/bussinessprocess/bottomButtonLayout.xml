<VerticalLayout>
    <VerticalLayout id="winHeader" spacing="true" margin="true" immediate="true" styleName="tx-win-header">
        <Label id="winHeaderLabel"  value="" styleName="tx6-win-header">
            
        </Label>
    </VerticalLayout>
    <TabSheet id="abstractTabSheet" immediate="true">
                       
    </TabSheet>
    <HorizontalLayout id="BottomBtnLayout" spacing="true" margin="true" >
        <Button id="previousBtn" caption="PREVIOUS"></Button>
        <Button id="nextBtn" caption="NEXT"></Button>
        <Button id="submitBtn" caption="SUBMIT"></Button>
        <Button id="closeButton" caption="CLOSE"></Button>
        <Button id="btnApprove" caption="APPROVE"></Button>
        <Button id="btnCancel" caption="CANCEL"></Button>
        <Button id="btnReject" caption="REJECT"></Button>
        <Button id="btnWithdraw" caption="WITHDRAW"></Button>
    </HorizontalLayout>
        
</VerticalLayout>
