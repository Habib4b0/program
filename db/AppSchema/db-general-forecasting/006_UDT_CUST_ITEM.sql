IF NOT EXISTS (
		SELECT 1
		FROM SYS.TABLE_TYPES
		WHERE NAME = 'UDT_CUST_ITEM'
		)
BEGIN
	CREATE TYPE [dbo].[UDT_CUST_ITEM] AS TABLE (
		[COMPANY_MASTER_SID] [int] NULL
		, [ITEM_MASTER_SID] [int] NOT NULL
		)
END
GO





